import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Liste e conclua',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
