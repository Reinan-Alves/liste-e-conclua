import { CompraConcluidaDirective } from './compra-concluida.directive';

describe('TarefaConcluidaDirective', () => {
  it('should create an instance', () => {
    const directive = new CompraConcluidaDirective();
    expect(directive).toBeTruthy();
  });
});
